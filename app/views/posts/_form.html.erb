<%= simple_form_for(@post) do |f| %>
  <%= f.error_notification %>
  <div class="field">
    <div class="control">
      <%= f.input :post, label: "Post about it", :required => false, input_html: { class: "textarea "}, wrapper: false, label_html: {class: "label"}, placeholder: "Compose a new post...", autofocus: true %>
    </div>
  </div>
  <%= f.button :submit, class: "button is-primary" %>
  <%= link_to 'Cancel', posts_path, class: "button is-light" if (params[:action] == 'edit') %>
<% end %>
